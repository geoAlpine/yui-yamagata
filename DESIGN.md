# やまがた結（ゆい） 設計書

> 住民が投稿し、住民が使う、山形県の生活情報共有サイト。
> 平時は雪、有事は災害。同じ器で運用し続けることで「必要な瞬間にそこに在る」状態をつくる。

作成: 2026-08-04 / ステータス: 初版ドラフト

---

## 0. 前提とする方針

本設計は以下の判断を前提にしている。異なる場合はこの章から見直す。

| 論点 | 採用した方針 |
|---|---|
| 対象時期 | **二段構え**（平時＝雪モード / 有事＝災害モード） |
| 運営主体 | **個人プロジェクトとして開始**。自治体連携は実物ができてから |
| 進め方 | **最小構成を先に動かす**。触りながら要件を確定 |
| 対象エリア | 山形県全域。初期の重点は内陸（山形市・上山・天童・寒河江）＋庄内（鶴岡・酒田） |
| インフラ | **既存のXserver VPSに同居**（4vCPU / 6GB / 150GB）＋ Cloudflare無料プラン。従量課金を持ち込まない |

---

## 1. なぜ作るのか（設計の出発点）

2026年の熊本地震で、御船町の個人が立ち上げた投稿型サイト「イマココナビ」が1日約7万人に使われた。営業中の店・ガソリンスタンド・給水場・炊き出し・使えるトイレを、住民が投稿し合うだけのサイトである。

これが機能したのは、機能が優れていたからではなく **必要になった瞬間にそこに在ったから** だ。同時にこれは弱点でもある。発災後に突貫で作られたものは、収束すると更新が止まる。2011年の `sinsai.info` も同じ経緯をたどり、現在は稼働していない。

したがって本プロジェクトのゴールは「熊本と同じものを山形で」ではない。

> **発災してから作らなくて済む状態を、平時のうちに用意しておくこと。**

この一文がすべての設計判断の基準になる。

### 既存サービスとの棲み分け

| 種別 | 例 | 本サイトの立場 |
|---|---|---|
| 行政の防災ポータル | こちら防災やまがた！、県河川・砂防情報、国交省防災ポータル | **競合しない。リンクする。** 避難所一覧・河川水位・気象警報は作らない |
| 避難所DB | 全国避難所ガイド | 同上。参照するだけ |
| 住民投稿型 | イマココナビ、サグリ社の熊本マップ | **ここを担う。ただし常設・平時運用という点で差別化** |

**作らないもの（重要）**: 避難所の網羅DB、河川水位、気象警報の再配信、安否確認。すべて既存のより信頼できる情報源がある。中核は「今どこで何が手に入るか」の一点に絞る。

---

## 2. 設計原則

1. **鮮度がすべて。** 災害時の生活情報は数時間で腐る。「昨日は開いてた」は無情報を通り越して有害。鮮度管理をUIの中心に置く
2. **平時に使われないものは有事にも使われない。** 冬に毎年開かれるサイトであることを最優先する
3. **速さ＞正確さ、ただし訂正可能に。** 事前審査はしない。事後の集合知で補正する
4. **公式を名乗らない。** 住民の目撃情報であり保証はしない、を常時明示する
5. **回線が細い前提で作る。** 停電・輻輳下で開けなければ意味がない
6. **投稿は3タップで終わる。** 被災地で長いフォームは埋められない
7. **不安を煽らない。** 開いた瞬間に恐怖を上乗せしない配色にする（下記）

### 配色

災害時に開くサイトで、警告色を強くしすぎるとそれ自体がストレスになる。**純黒・純白・原色の赤は使わない。**

- 背景は明るい生成り（`#fdfcf8`）。紙に近い落ち着き
- 文字は黒ではなく濃い墨茶（`#322e28`）。コントラストは確保しつつ刺さない
- 「悪い状態」は警告赤ではなく落ち着いた朱（`#aa5246`）。読めるが叫ばない
- 災害モードの帯も赤にしない。琥珀で示す

**ダークテーマは持たない。** 当初は夜間の眩しさを避けるため暖色のダークを用意したが、
実機で「暗すぎて不安」という反応があり削除した。`color-scheme: light` を宣言し、
端末がダークモードでも常に明るい画面を出す。

配色は全てWCAG AA（4.5:1）以上を実測で確認済み。

---

## 3. コアとなるデータモデル

### 3.1 「投稿一覧」ではなく「場所に観測が積み重なる」構造にする

イマココナビ型の素直な実装は「投稿の一覧」になる。しかしこれだと**同じ店に5人が投稿すると5件並び、どれが最新か分からない**。災害時に最も困る形だ。

そこで **スポット（場所）** と **観測（その時どうだったか）** を分離する。

```
spots  ── 場所。原則として使い回される。平時に事前登録できる
  │
  └─< observations  ── 「私は◯時にここを見て、こうだった」という一件の目撃
        │
        └─< confirmations  ── 他の人による追認 / 否認
```

これにより：

- 同じ店への報告が何件来ても一つのカードにまとまる
- 「12:00 開いてた → 15:00 閉まってた」という**状況の推移**が見える
- 平時にスポットを登録しておけば、有事は**状態を選んで送信するだけ**になる（新規投稿より圧倒的に速く、位置ズレも起きない）

### 3.2 スキーマ

```sql
-- 場所
spots (
  id            uuid pk,
  name          text,            -- 「ヤマザワ 南館店」
  category      text,            -- 下記カテゴリ参照
  location      geography(point),
  address       text,
  source        text,            -- 'official' | 'user' | 'imported'
  is_active     bool,            -- 閉店・廃止時に false
  created_at    timestamptz
)

-- 観測（一件の目撃情報）
observations (
  id            uuid pk,
  spot_id       uuid fk,
  status        text,            -- カテゴリごとに定義（3.4）
  observed_at   timestamptz,     -- ★実際に見た時刻。投稿時刻とは別物
  attrs         jsonb,           -- カテゴリ別の追加属性（3.4）
  note          text,            -- 自由記述。80字上限
  photo_path    text null,
  reporter_token text,           -- 端末ローカルID（匿名。荒らし抑制用）
  created_at    timestamptz
)

-- 追認・否認
confirmations (
  observation_id uuid fk,
  agrees         bool,           -- true=まだそう / false=違う
  reporter_token text,
  created_at     timestamptz,
  primary key (observation_id, reporter_token)
)
```

`observed_at` を `created_at` と分けるのは必須。圏外から戻ってきて後でまとめて投稿する人がいるため、「いつ投稿されたか」ではなく「いつ見たか」を表示しないと誤情報になる。

### 3.3 カテゴリ

**災害モード**

| カテゴリ | TTL | 状態 | 追加属性 |
|---|---|---|---|
| ⛽ ガソリンスタンド | 120分 | 給油できる / 数量制限あり / 在庫なし / 閉まっている | **待ち時間**、給油上限L |
| 🛒 スーパー・コンビニ | 360分 | 営業中 / 一部営業・品薄 / 休業 | 待ち時間 |
| 🚰 給水所 | 720分 | 実施中 / 予定あり / 終了 | 容器持参要否、**水の種類（給水車/水道/井戸水）** |
| 📦 物資配布・炊き出し | 360分 | 実施中 / 予定あり / 終了 | 配布物 |
| 🚻 トイレ・入浴 | 720分 | 使える / 使えない | 無料/有料 |
| 🔌 充電できる場所 | 240分 | 充電できる / 混雑 / 使えない | 待ち時間、種類（コンセント/USB/給電車） |
| 📶 携帯・Wi-Fi | 240分 | 無料Wi-Fi / つながる / つながりにくい / 圏外 | キャリア |
| 🏥 医療機関・薬局 | 360分 | 診療・営業中 / 一部のみ / 休診 | 対応内容 |
| 🏧 ATM・現金 | 360分 | 使える / 行列あり / 使えない | 待ち時間 |
| 🧺 コインランドリー | 720分 | 使える / 混雑 / 使えない | — |
| 🗑️ 災害ごみ仮置場 | 1440分 | 搬入できる / 混雑 / 受付終了 | 待ち時間、受け入れ品目 |
| 💡 断水・停電 | 1440分 | 停電中 / 断水中 / 復旧 | — |
| 🚧 通行止め・道路被害 | 720分 | 通行止め / **冠水** / 片側通行 / 通行可 | — |

イマココナビには無く、実需から追加したもの: **充電・携帯/Wi-Fi・医療/薬局・ATM・コインランドリー・災害ごみ仮置場**。

- **充電**と**携帯/Wi-Fi**は、停電下で「次の情報を得る手段」そのものを左右する。他のどの情報よりも上流にある
- **ATM**は停電でカード決済が止まると現金が要る場面が必ず来る
- **災害ごみ仮置場**は水害復旧で需要が跳ねる。令和6年7月豪雨では県内で床上浸水427棟。片付けごみの持込先は実際に大きな需要になる
- **冠水**を道路の状態に追加。山形は最上川水系の水害が現実的な脅威で、通行止めとは危険度が違う

**平時（雪）モード**

| カテゴリ | 状態 | 追加属性 |
|---|---|---|
| 除雪状況 | 除雪済 / 未除雪 / 作業中 | — |
| 路面状況 | 圧雪 / アイスバーン / 乾燥 | — |
| 通行止め・立ち往生 | 通行止め / 渋滞・立ち往生 / 通行可 | — |
| 雪下ろし業者 | 受付中 / 満杯 | 連絡先、目安価格 |

ガソリンの**待ち時間**は自由記述ではなく構造化する。熊本でも東日本でも、最も需要が高く最も腐りやすい情報がこれだった。

### 3.4 鮮度管理（本サイトの心臓部）

各カテゴリに **TTL（情報の賞味期限）** を定める。

| カテゴリ | TTL |
|---|---|
| ガソリンの待ち時間 | 2時間 |
| スーパー・コンビニ営業 | 6時間 |
| 給水所 | 12時間 |
| 物資配布・炊き出し | 予定終了時刻まで |
| トイレ・入浴 | 12時間 |
| 断水・停電 | 24時間 |
| 通行止め | 12時間 |
| 除雪状況 | 12時間 |

表示は経過時間に応じて三段階に減衰させる。

```
経過 < TTL/3        → 通常表示。状態色（緑/黄/赤）をそのまま出す
TTL/3 〜 TTL        → 彩度を落とす + 「情報が古い可能性」+ 再確認ボタンを強調
経過 > TTL          → 状態を「不明」に戻す。灰色表示。履歴には残る
```

**再確認は1タップで完結させる。** カードに「まだこの状況ですか？」と出し、[はい] / [違う] の二択のみ。押した瞬間に新しい観測として記録される。これがあるかないかで情報の生存率がまるで変わる。

「違う」が押されたら、続けて正しい状態を選ぶ画面に進む（任意。スキップ可）。

---

## 4. モード切替

サイト全体に **平時モード / 災害モード** を持たせる。

- **切替は管理者操作**。ただし気象庁の警報・特別警報や震度情報を監視し、条件を満たしたら管理画面に「災害モードに切り替えますか？」と提案を出す（自動切替はしない。誤爆が致命的なため）
- モードが変わるのは **表示カテゴリのセットと初期表示順** だけ。UI構造・操作手順・URLは変えない
- 平時モードでも災害カテゴリへは手動で到達できる（局所的な停電・断水に対応するため）

平時に雪の報告で慣れた操作が、そのまま災害時に使える。これが二段構えの実利であって、単に「冬も何か表示しておく」ことではない。

---

## 5. 画面構成

```
/                     トップ：近い順リスト（既定）／地図切替
/spots/:id            スポット詳細：現在の状況＋観測履歴＋報告ボタン
/report/:spotId       状況を報告（3タップ）
/spots/new            新しい場所を追加
/info                 公式情報リンク集・このサイトについて・免責
/admin                管理（認証あり）
```

### 5.1 トップは地図ではなくリストを既定にする

直感に反するが、災害時にはこれが正しい。

- 地図タイルは重い。輻輳した回線で真っ先に開けなくなる
- 「近くのどこが開いてるか」は**現在地から近い順のリスト**で十分すぎるほど答えられる
- 地図は俯瞰したいときの任意機能。ワンタップで切り替えられれば足りる

リストの1カードに載せる情報（優先度順）:

```
┌──────────────────────────────┐
│ ⛽ ○○石油 △△店        1.2km │
│ ● 給油可・待ち20分            │
│ 15分前に確認 ・ 3人が同意     │
│ [まだこの状況？ はい / 違う]   │
└──────────────────────────────┘
```

**「◯分前に確認」を店名の次に目立たせる。** 状態そのものより鮮度のほうが判断材料として重要な場面が多い。

### 5.2 報告フォームは3タップ

スポットが既に存在する前提なら、カテゴリは既知。したがって:

1. スポットを選ぶ（リストのカードから直接入るので実質0タップ）
2. 状態を選ぶ（大きなボタン3〜4個）
3. 送信

追加属性・メモ・写真はすべて任意で、送信ボタンより下に置く。**入力せずに送信できることを最優先**にする。被災地で長いフォームは埋められない。

`observed_at` は既定で「今」。「さっき見た情報を今送っている」ケース用に、[今 / 30分前 / 1時間前 / 時刻指定] のチップを置く。

---

## 6. 誤情報・荒らし対策

事前審査はしない。速度が命であり、モデレーターを24時間張り付けられる体制もないため。事後の集合知と限定的な人力で対処する。

| 手段 | 内容 |
|---|---|
| 追認・否認 | `confirmations` の集計。否認が追認を上回ったら自動でグレー表示＋「情報が食い違っています」を出す |
| **署名付き匿名ID** | ユーザー登録は求めない。サーバが HMAC 署名した値を **HttpOnly cookie** で配り、投稿者IDとする（`lib/identity.ts`） |
| IPによる歯止め | cookie は消せば作り直せるため、追認・場所登録は実IPでも回数を制限する |
| レート制限 | 同一トークン・同一スポットへの短時間連続投稿を制限 |
| 写真 | 任意だが強い根拠。行列や貼り紙の写真1枚が説得力を持つ |
| 通報 | 各観測に通報ボタン。管理画面のキューに積む |
| 緊急削除 | 人命に関わる誤誘導（避難経路・救助関連）は管理者が即時削除。これだけは能動的に監視する |
| 免責の常時表示 | ヘッダーに「住民の目撃情報です。公式情報ではありません」を固定 |

`sinsai.info` はボランティアのモデレーターが公開前に真偽を確認する運用だった。本サイトは規模と体制が違うため事後型を採るが、**災害モード中は管理画面の通報キューを1日数回見る**運用は最低限必要になる。誰がやるかは実運用前に決めること。

### 匿名IDをクライアントに作らせない理由

イマココナビは Firebase の匿名認証（`signInAnonymously`）を使っており、自分でアカウントを作る導線を持たない。ログインがあるのは管理者だけ。**この構えは正しい**ので踏襲する。被災地でアカウント作成を挟んだ瞬間、使われるサイトではなくなる。

ただし識別子の出どころが違うと強度が変わる。

| | イマココナビ | 本サイト |
|---|---|---|
| 匿名IDの発行 | Firebase が発行し署名 | **サーバが HMAC 署名し HttpOnly cookie で配る** |
| クライアントからの詐称 | できない | できない |

localStorage の UUID をそのまま信じる実装にすると、**ランダムな文字列を100個投げるだけで「100人が同意」を捏造できる**。追認は情報の信頼性を担保する仕組みなので、ここが偽装できると設計の土台が抜ける。

**発行のタイミングに注意。** middleware でページ応答に `Set-Cookie` を載せてはいけない。一覧ページは Cloudflare で30〜60秒キャッシュするため、**キャッシュに `Set-Cookie` が焼き付くと全員が同じトークンを共有する**。よって「最初の書き込み時に API レスポンスで発行する」方式をとる（API は常に `no-store`）。

### 管理者認証

環境変数のパスワード1本という最小構成（`lib/admin.ts`）。ユーザー管理は作らない。運用者が1〜2名である間はこれで足り、増えたときに作り直せばよい。cookie は12時間で失効させる（共用端末での置き忘れ対策）。

必要な環境変数:

```
TOKEN_SECRET=<十分に長いランダム文字列>   # 匿名ID・管理cookieの署名鍵。本番では必須
ADMIN_PASSWORD=<管理画面のパスワード>
```

---

## 6.5 お知らせ（notices）— 2つ目にして最後のプリミティブ

イマココナビが持っていた **ボランティア募集 / 物資の要望 / 支援情報** をどう扱うか。

これらは「場所の今の状況」ではない。通りすがりの誰かが目撃するものではなく、**主体が期間を区切って出す告知**である。observations に相乗りさせると型が壊れる。かといってイマココナビのようにコレクションを増やすと、`posts / lifeline / shien / volunteer / requests` と5つに分岐し、それぞれ別の描画・別のフォームを持つ状態になる。

そこでプリミティブを**2つに畳む**。3つ目は作らない。

| | spots ← observations | notices |
|---|---|---|
| 何を表すか | 場所の今の状況 | 主体が出す告知 |
| 投稿者 | **匿名でよい** | **記名必須**（団体名＋連絡先） |
| 速度 | 2タップで完結させる | あえて摩擦を残す |
| 寿命 | カテゴリ別TTLで自動失効 | 掲載期限で自動失効（3/7/14/30日） |
| 間違えたときの害 | 「店が開いてなかった」で済む | **詐欺・転売・善意の殺到の入口になる** |

**匿名性の扱いを正反対にすることが設計の核心。** 状況報告は匿名でよいが、「ここに物資を送ってください」を匿名で出せてはいけない。

### ここが弱点になる（実装済みの対策と、残っているリスク）

| 弱点 | 対策 | 残るリスク |
|---|---|---|
| **物資の要望が詐欺・転売に使われる** | 団体名・連絡先を必須。個人宅への送付先は掲載不可と明記 | 団体名は自称。管理者が連絡先の実在を確認したものだけ「確認済み」を出す運用が要る |
| **個人のボランティア募集で現地に人が殺到し、受け入れ側が壊れる** | 投稿フォームで、まず社協の災害VCへの相談を促す。県の災害ボランティア情報へリンク | 掲載自体は止められない。緊急削除の運用に頼る |
| **募集が終わったのに残り続ける** | 掲載期限を必須にし、期限切れは一覧から自動で落とす | 期限内に充足した場合は残る。終了報告の導線が未実装 |
| **「確認済み」バッジが信頼の誤認を生む** | 既定は「未確認」。付けるのが例外であることを一覧に明記 | 未確認でも上位に出ると信じられてしまう。並び順は確認済みを優先 |
| **井戸水の飲用可否** | 給水所の水の種類を区別し、井戸水にはカード上で注意を常時表示 | 「煮沸すれば安全」という誤解は消せない。自治体案内への誘導のみ |
| **医療情報の誤りは命に関わる** | 「診療中/一部/休診」の粗い状態のみ。診療科目や受入可否は扱わない | 緊急時は119という導線が未実装 |
| **お気に入りが端末を変えると消える** | 仕様として受け入れ、画面上で明示 | 端末を失った被災者には引き継げない |

---

## 7. 技術構成

**方針: 従量課金を一切持ち込まない。** 災害時に請求が青天井になる構成は、個人運営では続かない。イマココナビがクラウドファンディングに追い込まれたのは機能の問題ではなく、この構造の問題だった。既存のXserver VPS（固定費）に載せることで、負荷がいくら跳ねても月額は変わらない形にする。

### 全体構成

```
                                         ┌──────────────────────┐
  利用者 ──→ Cloudflare (東京/大阪) ──→ │ Xserver VPS          │
             ・HTMLを30〜60秒キャッシュ  │ 4vCPU / 6GB / 150GB  │
             ・静的アセットを長期キャッシュ│                      │
             ・DDoS防御 / IP秘匿         │  nginx (既存)         │
             ・転送量無制限・無料         │    └→ :3010 bousai   │
                     │                   │         (systemd)     │
              到達は5〜10%だけ ──────────→│    PostgreSQL+PostGIS│
                                         │  ※既存9サイトと同居   │
                                         └──────────────────────┘
```

### 選定

| 層 | 選定 | 理由 |
|---|---|---|
| フレームワーク | Next.js (App Router) + TypeScript、`output: 'standalone'` | SSRで初期表示が軽い。standaloneならVPSで `node server.js` だけで動く |
| プロセス管理 | **systemd**（PM2ではなく） | `MemoryMax` / `CPUQuota` でリソース上限を強制できる。再起動後も自動復帰 |
| DB | **PostgreSQL + PostGIS**（VPSに直接インストール） | 地理検索がSQLで書ける。既存のMySQLには触らない |
| リバースプロキシ | 既存のnginxに `server` ブロックを追加 | 既存9サイトと同じ運用に揃える |
| CDN / 前段 | **Cloudflare 無料プラン** | スパイクの吸収・DDoS防御・IP秘匿・転送量無制限。すべて無料 |
| 地図 | MapLibre GL + **国土地理院タイル** | 無料・国産・利用規約が明確。Google Mapsは災害時のアクセス急増で課金が読めない |
| 画像 | ローカルディスク（`/var/www/bousai/uploads`）。クライアント側で長辺1280px・WebPに圧縮してから送信 | 92GB空きがある。オブジェクトストレージの従量課金を避ける |
| 認証 | 一般利用者は**不要**。管理者のみ | 投稿の速度を落とさない |

**Docker を使わない理由**: 一人で運用する以上、災害の夜中に触る層は少ないほうがいい。既存サイトがすべて nginx + Node + systemd/PM2 で動いているので、同じ形に揃える。ただしコードは環境依存を持たせず、Phase 2で別VPSに切り出すときにそのまま移せるようにしておく（設定は環境変数のみ、パスは相対）。

### 既存サイトを巻き添えにしないための制約

このVPSでは商用サイト（yukemuri-coffee.com / smart-agri-vision.net / geoalpine.net ほか）が本番稼働している。**防災サイトが暴走しても、それらのCPUとメモリは絶対に侵さない。**

実測値: CPU idle 94〜97%、メモリ使用率50%（commit 43%）、空き2.5GB。余裕はあるが、上限は明示的に縛る。

```ini
# /etc/systemd/system/bousai.service
[Service]
MemoryMax=768M          # 超えたらこのサービスだけOOMで殺される
MemoryHigh=640M         # ここから積極的に回収
CPUQuota=150%           # 4コア中1.5コアまで
TasksMax=256
Restart=always
RestartSec=5
```

PostgreSQL も同居前提で控えめに設定する（既定値のままだとメモリを取りすぎる）。

```
shared_buffers = 256MB
work_mem = 8MB
max_connections = 40        # Next.js側でコネクションプールを持つので少なくてよい
effective_cache_size = 1GB
```

### Cloudflare の設定（ハマりどころ3つ）

順番が重要。**①を最初にやらないと事故る。**

**① 実IPの復元 — これを忘れるとfail2banがCloudflareをBANしてサイトが全滅する**

全アクセスがCloudflare経由になるため、nginxのログに残るIPがすべてCloudflareのものになる。このVPSでは `fail2ban` と `auto-block-attackers.sh`（毎時）が動いているので、放置すると**Cloudflareのアドレス帯をブロックし、結果として全利用者から見えなくなる**。

```nginx
# /etc/nginx/conf.d/cloudflare-realip.conf
# 一覧は https://www.cloudflare.com/ips-v4 / ips-v6 から取得して生成する
# （手打ちしない。範囲は変わる。cronで月1更新する）
set_real_ip_from  <Cloudflare の各レンジ>;
real_ip_header    CF-Connecting-IP;
```

`auto-block-attackers.sh` 側も、参照するログのIPが実IPになっていることを確認する。

**② HTMLは既定でキャッシュされない**

Cloudflareが自動でキャッシュするのは画像・CSS・JSのみ。**HTMLは素通し**なので、何もしないとスパイク対策にならない。Cache Rules で明示的に指定する。

| 対象 | キャッシュ |
|---|---|
| `/`、`/spots/*`（一覧・詳細） | **30〜60秒**（`s-maxage=60, stale-while-revalidate=300`） |
| `/_next/static/*`、画像 | 1年（内容ハッシュ付きファイル名） |
| `/report/*`、`/api/*`（書き込み系） | キャッシュしない |

鮮度は分単位で足りるので、秒単位の即時性は不要。60秒キャッシュでオリジン到達は5〜10%になる。

**③ SSLは必ず Full (strict)**

既にLet's Encrypt + certbotが動いているため。`Flexible` にすると Cloudflare→VPS が HTTP になり、nginxの301リダイレクトと衝突して**リダイレクトループ**になる。

### キャッシュを効かせるためのファイル分割

イマココナビは全部を1つの270KB HTMLに入れていた（画像もbase64で本文に埋め込み）。結果、**テキストを1文字直すと270KB全部が再ダウンロード**になる。災害中は文言修正が頻発するので、これは効いてくる。

本サイトでは逆にする。

- HTML本体は軽く保ち、短命キャッシュ（60秒）
- CSS/JS/画像は内容ハッシュ付きファイル名にして**1年キャッシュ**
- 画像をHTMLに埋め込まない

これで文言修正しても再送されるのはHTMLだけになる。

### 通信が細い/切れる状況への対策

- 初期HTMLだけで主要情報が読める（JSが落ちても最低限見える）
- **PWA + Service Worker**：圏内で見た情報を圏外でも再表示できる。「最終取得: ◯時◯分」を明示
- 地図は遅延読み込み。既定では読み込まない
- ページ全体の初期転送量の目標: **100KB以下**（画像除く）

### 縮退運転

負荷が想定を超えた場合に、機能を落として生き残る順番をあらかじめ決めておく。

1. 地図を無効化（リスト表示のみ）
2. 写真の表示をサムネイルのみに
3. 投稿を一時停止し、閲覧専用にする（**閲覧だけは最後まで守る**）
4. Cloudflare の「Always Online」でキャッシュから配信し続ける

管理画面から段階的に切り替えられるようにしておく。

---

## 8. 初期データの投入

平時にスポットを揃えておくことが、有事の使い勝手を決める。以下から初期投入する。

- **OpenStreetMap**（ODbL。出典表示で商用含め利用可）: スーパー、コンビニ、ガソリンスタンド、公衆トイレ
- **経産省「住民拠点SS」リスト**: 自家発電設備を備え停電時も給油できるスタンド。**災害時に最も価値の高いデータ**なので必ず入れる。専用アイコンで区別する
- **各市町村の給水拠点・避難所**: 公開データを参照。避難所そのものはリンクに留めるが、給水拠点はスポット化する
- **温浴施設**: 山形は温泉が多く、断水時の入浴先として実効性が高い

ライセンスは必ず確認し、`/info` に出典を明記する。

---

## 9. ロードマップ

### Phase 0 — MVP（まずここまで作る）

**先行するインフラ準備**（アプリより前にやる）
- VPSのカーネル更新と再起動（未決事項7）
- PostgreSQL + PostGIS の導入、`shared_buffers` 等を同居前提に調整
- ドメイン取得 → Cloudflare登録 → **`set_real_ip_from` 設定（最優先）** → SSL Full (strict) → Cache Rules
- systemd unit を `MemoryMax` / `CPUQuota` 付きで用意

**アプリ**
- スポット / 観測 / 追認のデータモデル
- 近い順リスト、スポット詳細、3タップ報告
- 鮮度の減衰表示と1タップ再確認
- 災害モードのカテゴリのみ
- 対象エリア: 山形市周辺に限定して初期データ投入
- 管理画面は通報キューと緊急削除だけ

**Phase 0 で意図的に作らないもの**: 地図、PWA、雪モード、モード自動切替、写真投稿。

### Phase 1 — 冬に向けて（〜2026年11月）
- 雪モードのカテゴリとモード切替
- 地図表示（MapLibre + 地理院タイル）
- PWA / オフライン表示
- 写真投稿
- 県全域にスポット拡大
- **この冬、実際に雪で運用してみる。ここが最大の検証機会**

### Phase 2 — 有事対応の強化
- 気象警報の監視とモード切替の提案
- 公式情報（県・市町村）のフィード取り込みと参照表示
- 自治体・地元団体への持ち込み（実物と冬の運用実績を持って）

---

## 10. 未決事項

実装前に決める必要があるもの。

1. **運営者の明示**: サイト運営者名をどう出すか。個人名か屋号か。免責と信頼性の両方に効く
2. **災害モード中のモデレーション当番**: 誰が通報キューを見るか。一人では持たない
3. **ドメイン**: 覚えやすさが生死を分ける。短く、山形と分かるもの
4. **平時の周知手段**: 冬に使ってもらうための入口。地元メディア、SNS、市町村の広報
5. **バックアップ先**: 同じVPS内に置いてもバックアップにならない。投稿データを日次で**外部**に逃がす先を決める（別VPS / 自宅 / オブジェクトストレージ）
6. **Cloudflare依存の受容範囲**: 通信をCloudflareが終端するため、投稿内容と投稿者IPが同社を通過する。加えてCloudflare自体の障害時は巻き添えになる。DNSのTTLを短くし、**VPS直結に戻す手順**を文書化しておく
7. **VPSの再起動タイミング**: 稼働カーネルが未適用のまま（6.8.0-111 / 導入済み6.8.0-136、94日無再起動）。防災サイトを載せる**前に**適用しておく

---

## 付録: 判断の記録

- **なぜ地図を既定にしないか** → 重い。かつ「近くのどこが開いてるか」はリストで答えられる
- **なぜログイン不要か** → 投稿の速度が最優先。荒らし対策はトークンと事後補正で代替する
- **なぜ避難所DBを作らないか** → 全国避難所ガイド・県サイトのほうが正確。二重管理は誤情報の元
- **なぜ雪を平時ユースケースに選ぶか** → 山形で毎年必ず来る「災害」であり、操作を習慣化できる唯一の題材だから
- **なぜモード自動切替をしないか** → 誤爆したときの信頼失墜が回復不能。提案までに留める
- **なぜマネージドサービス（Vercel/Supabase/Firebase）を使わないか** → 従量課金だから。災害時にアクセスが跳ねると請求も跳ねる。手持ちのVPSなら固定費で済む
- **なぜDockerを使わないか** → 一人で運用する以上、夜中に触る層は少ないほうがいい。既存9サイトと同じ nginx + Node + systemd に揃える

### 付録B: イマココナビ（令和8年熊本地震）の実装分析から得た教訓

2026-08-04時点の配信ソースを直接確認した結果。本設計の根拠として残す。

**構成**: 単一HTML 270KB（インラインJS 94KB / CSS 12KB / base64画像 135KB）、ビルドなし、フレームワークなし、Firebase（Firestore + 匿名認証）直叩き、地図は自前で作らず他社マップへ外部リンク。

| 観測された問題 | 本設計での対応 |
|---|---|
| **座標を持っていない**（`lat`/`lng`/`GeoPoint`が一切ない）。位置は `area` というテキスト一本、ソートは `createdAt desc` のみ。「近い順」が原理的に出せない | **座標を必須にする。** 地図は後から足せるが、座標のないデータは遡って作れない |
| 鮮度判定が `Date.now()-created > 12*3600000` の二値のみ。カテゴリ一律で、失効もしない | **カテゴリ別TTL + 3段階減衰 + 失効**（3.4節） |
| 投稿がフラット（`posts`）。同じ店に2人が投稿すると2件並び統合できない。`updates`配列で後付けの延命をしている | **spots / observations の分離**（3.1節） |
| 5コレクション（posts/lifeline/shien/volunteer/requests）がそれぞれ別の描画・別のフォーム。災害中の機能追加で積み増した跡 | 平時に作れる利点を活かし、**単一モデルに畳んでおく** |
| 270KBが1つのキャッシュ単位。文言修正で全量再送 | **HTML / 静的アセットを分離**（7節） |
| Firestore従量課金 → クラウドファンディングに至る | **VPS固定費**（7節） |

**逆に真似すべき割り切り**: 地図を自前で作らない、認証は匿名のみ、ビルド構成を軽くする。速さの正体は書いた速度ではなく「作らなかったものの量」であり、Phase 0では同じ割り切りを採用する。**ただしデータの形だけは妥協しない** — そこだけは後から直せない。
