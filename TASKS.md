# 残課題

最終更新: 2026-08-04 / Phase 0（MVP）実装中

設計の根拠は [DESIGN.md](./DESIGN.md)、動かし方は [README.md](./README.md) を参照。
ここには**まだ終わっていないもの**だけを、決める人ごとに分けて置く。

---

## 完了しているもの（参考）

| | |
|---|---|
| データモデル | spots ← observations ← confirmations / notices |
| 鮮度管理 | カテゴリ別TTL・3段階減衰・1タップ再確認 |
| 画面 | 一覧（距離順）・詳細・報告・場所の追加・お知らせ・管理 |
| 匿名ID | HMAC署名付き HttpOnly cookie（登録不要のまま詐称を防ぐ） |
| 管理 | モード切替・通報キュー・緊急削除・お知らせの確認 |
| 実データ | 山形県 2,795件・市町村つき100% |
| 配色 | ライト固定・WCAG AA 実測確認済み |

---

## A. あなたにしか決められないこと

**これが決まらないと先に進めない、または進めても手戻りになる。**

### A-1. ドメイン ✅完了
サイト名は **やまがた結（ゆい）**、ドメインは **`yui-yamagata.com`**（取得済み）。

「結」は農村の相互扶助の慣行。住民が報告し合って支え合うという仕組みそのものを指す。
読みが割れる字なので、画面では常に「やまがた結（ゆい）」と読み仮名を添える
（**災害時に口頭で伝わることが生命線**のため）。

残件: `yui-yamagata.jp` も空いたまま。**なりすまし防止のため押さえておくのが望ましい**
（似た名前の別ドメインを第三者に取られると、災害時に偽サイトの入口になる）。
年数千円でリダイレクトを張るだけ。必須ではないが安い保険。

### A-2. モデレーション当番 — 誰が通報キューを見るか
事前審査をしない設計を採った以上、**ここが唯一の人力の防波堤**。
災害モード中は1日数回見る必要があり、一人では持たない。
決まらないなら「災害モード中は投稿を止めて閲覧専用にする」という代替案もある。

### A-3. お知らせの「確認済み」を誰が付けるか
団体名は自称。連絡先に実際に連絡して確認する作業が要る。
A-2と同じ人でよいが、**確認しないなら「確認済み」バッジ自体を外す**判断もある
（付かないことが常態なら、バッジは誤解を生むだけ）。

### A-4. 運営者の表示
個人名か屋号か。免責の実効性と信頼性の両方に効く。

### A-5. 初期転送量の目標をどうするか（保留中の判断）
設計書に「100KB以下」と書いたが、実測 **186KB**（gzip）で未達。
Next.js + React の下限が約150KBあり、この構成では到達できない。

- 案1: 目標を実測に合わせて改める（HTML 15KB以下・JS無しで読める、を必達に）
- 案2: 100KBを死守するため Next.js をやめ、素のSSR + 最小限のJSにする（1〜2日）

**HTML 9.3KB に店名・状態・鮮度・免責が全部入っており、JSが来る前に読める**状態は
既に達成している。私は案1を推すが、これは方針の話なのであなたの判断。

### A-6. Cloudflare 依存を受け入れるか
通信をCloudflareが終端するため、**投稿内容と投稿者IPが同社を通過する**。
またCloudflare自体の障害時は巻き添えになる。
受け入れる場合はDNSのTTLを短くし、VPS直結に戻す手順を文書化しておく。

---

## B. 私が進められる実装

### B-1. 空カテゴリの表示方針 ★次にやるならこれ
13カテゴリのうち**6つが平時は0件**（給水所・物資配布・携帯Wi-Fi・断水停電・通行止め・災害ごみ）。
これらはOSMに無いのではなく、**自治体が災害時に発表するもので事前に埋められない**。
空のタブが並ぶと「使われていないサイト」に見える。

対応案: 平時は隠す / 「発災時に情報が集まります」と明示する / 災害モードでのみ出す。

### B-2. 雪モードのデータモデル ★冬までに必要
除雪・路面・通行止めは**道路の「区間」**だが、現在のモデルは点しか持てない。
「県道19号の◯◯地区〜◯◯地区が未除雪」を表現できない。

PostGIS の LineString を持たせるか、点の連なりで妥協するかの設計判断が要る。
**Phase 1（この冬）の本丸**。

### B-3. 写真投稿
行列や貼り紙の写真1枚が持つ説得力は大きい。
クライアント側で長辺1280px・WebPに圧縮してから送る前提（DESIGN.md 7章）。

### B-4. 応急給水拠点の取り込み
各市町村が公開している給水拠点。OSMには無い。
市町村ごとに形式がバラバラなので、まず数自治体で試す。

### B-5. カテゴリ13個が多すぎないか検証
実機で触ってみないと判断できない。冬の運用で「どれが実際に投稿されるか」を見て削る。

### B-6. 本番デプロイ一式
- PostgreSQL + PostGIS の導入と同居チューニング（`shared_buffers=256MB` 等）
- systemd unit（`MemoryMax=768M` / `CPUQuota=150%`）
- nginx の server ブロック追加
- `TOKEN_SECRET` / `ADMIN_PASSWORD` の本番値生成
- Cloudflare 設定（**`set_real_ip_from` を最優先**→ SSL Full(strict) → Cache Rules）

---

## C. 手を動かせば終わるが、材料が要るもの

### C-1. 住民拠点SS の反映
スクリプト（`scripts/import_juminss.mjs`）は完成し、名寄せ・ジオコーディング・
誤検出の排除まで検証済み。**あとは資源エネルギー庁の一覧CSVを置くだけ**。

エネ庁のサイトが自動取得を弾く（302/403）ため、手動ダウンロードが必要。
山形県の行だけを「SS名」「所在地」列を含むCSVで `data/` に保存して実行する。

### C-2. バックアップ先
同じVPS内に置いてもバックアップにならない。
投稿データを日次で**外部**に逃がす先を決める（別VPS / 自宅 / オブジェクトストレージ）。

---

## D. VPSの宿題（防災サイトとは独立）

セキュリティ監査（2026-08-04）で挙げた未対応分。**1が最優先。**

| | 内容 |
|---|---|
| **D-1 🔴** | **カーネル未適用のまま94日稼働**（6.8.0-111 / 導入済み 6.8.0-136）。Docker関連27パッケージも未適用。防災サイトを載せる**前に**再起動しておく |
| D-2 🟡 | GitHub Actions のデプロイ鍵が sudo 所属ユーザーに刺さっている。専用ユーザーか `command=` 制限へ |
| D-3 🟡 | HSTS ヘッダがない |
| D-4 🟡 | Node 3プロセスが `0.0.0.0` バインド（外部からは遮断済みだが二重防御に） |
| D-5 🟢 | `既存案件のユーザー` の `www-data` グループ所属 |
| D-6 🟢 | 用途不明のシェル付きユーザー `musicgen` / `hailo` |
| D-7 🟢 | 死んだ `proxy_pass 127.0.0.1:54321`（将来そのポートを別プロセスが取ると意図せず公開される） |

VPSの契約は**現状維持が正解**。4GBプラン価格で6GB載っている条件は、
2026-07-14の増強停止により二度と取得できない。期間変更も「新規申込」扱いに
ならないかサポート確認が要る。

---

## E. 平時の周知（技術では解決しない）

「発災してから作らなくて済む状態を平時に用意する」がこのプロジェクトの目的である以上、
**冬に実際に使われるかどうかが唯一の検証**になる。

- 地元メディア・SNS・市町村の広報への持ち込み
- 自治体連携は「動いている実物」と冬の運用実績ができてから

---

## 優先順位（私の見立て）

1. **A-1 ドメイン** — これが無いと本番に進めない
2. **B-1 空カテゴリの表示方針** — 軽い割に見え方が大きく変わる
3. **D-1 カーネル再起動** — 本番を載せる前に済ませたい
4. **B-2 雪モードのデータモデル** — 冬に間に合わせるなら早めに着手
5. **A-2 / A-3 運営体制** — 公開前に必ず決める。技術で代替できない

---

## F. 検討のみ（作らない）

### F-1. 県外への展開
「一応の検討」であり、**今は実装しない**。ただし判断の記録として残す。

**技術的には何が要るか**

| | 現状 |
|---|---|
| `regions` テーブルと `region_id` | 無い。後付けは報告データが溜まってからだと苦行（座標と同じ話） |
| `site_state` を地域ごとに | 現在1行固定。**山形の地震で宮城が災害モードになってはいけない** |
| 山形固有値の外出し | 県境のbbox・`ISO3166-2=JP-06`・市町村13件・公式リンクがハードコード |

**それより先に壁になるのは運用のほう。**
通報キューを見る人、「確認済み」を付ける人は、**その土地を知っている人でないと判断できない**。
「◯◯町の△△商店」が実在するかは地元の人にしか分からない。
つまり県外展開は「システムを広げる」話ではなく「各県に当番を見つける」話になる。
A-2（山形のモデレーション当番）すら未決なので、そこが解けない限り2県目は動かない。

**名前の扱い**: サイト名に山形を入れたため、県外へ出るなら改名が要る。
それが嫌なら地域非依存のドメイン（`yui-seikatsu.jp` など）を押さえて寝かせておく手はある。
年数千円で改名の可能性を買える、という程度の話。

### F-2. 一年中使ってもらうための拡張
雪モードが効くのは12〜3月で、**4〜11月の8ヶ月は開く理由がない**。
ここを埋めないと結局イマココナビと同じ道をたどる。検討した案:

- **クマ・野生動物の出没**（春〜秋・命に関わる・鮮度管理がそのまま活きる）← 最有力
- **過疎地の店・GSの営業状況**（通年・**データは既にある**ので追加実装ほぼゼロ）
- 道路の通行止め・工事（通年）
- 道の駅・直売所（春〜秋）

仕組みとしては `site_state.mode` を2値から**季節モード**に広げるだけで、
実装済みの器（TTL・再確認・距離順・報告フォーム）はそのまま使える。

**線引きの原則**: その場所の「今」を知らずに行くと、無駄足になるか危険があるか。
観光情報やイベント告知は入れない（災害時に必要な情報が埋もれる）。
