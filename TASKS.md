# 残課題

最終更新: 2026-08-05 / **本番稼働中** https://yui-yamagata.com

設計の根拠は [DESIGN.md](./DESIGN.md)、動かし方は [README.md](./README.md)。
ここには**まだ終わっていないもの**だけを、決める人ごとに置く。

---

## 稼働しているもの

| | |
|---|---|
| 本番 | https://yui-yamagata.com （実データ 2,822件） |
| ステージング | https://staging.yui-yamagata.com （Basic認証・noindex・確認用の帯） |
| CI | 型・lint・テスト24件・PostGIS込みの実起動確認・免責/出典/noindexの固定 |
| CD | `stg`→ステージング / `main`→本番。CI通過時のみ。失敗時は自動切り戻し |
| 監視 | 6時間ごとに外から検査。異常はIssue化、復旧で自動クローズ |
| データ更新 | 毎月1日3時にOSM再取り込み（cron登録済み・`--check` で検証済み） |
| インフラ | PostgreSQL+PostGIS / systemd（768M・150%上限）/ Let's Encrypt / HSTS |

**方針転換（2026-08-05）**: 雪モードを廃止し災害専用にした。
平時の「今どこが開いてるか」はGoogleのほうが正確で、そこに勝負を挑む理由がない。
うちが勝てるのは公式情報が実態と食い違うときだけで、それは災害時に限られる。
経緯は `lib/categories.ts` の冒頭コメント。

---

## A. あなたにしか決められないこと

### A-1. モデレーション当番 ★最重要・公開前に必須
事前審査をしない設計を採った以上、**通報キューを見る人がいなければこの設計は成立しない**。
技術で代替できない唯一の部分。災害モード中は1日数回見る必要があり、一人では持たない。

決まらない場合の代替案: **災害モード中は投稿を止めて閲覧専用にする**。
守れない体制で投稿を受けるより、そのほうが誠実。

### A-2. お知らせの「確認済み」を誰が付けるか
団体名は自称。連絡先に実際に連絡して確認する作業が要る。
**確認しないなら「確認済み」バッジ自体を外す**判断もある。
付かないことが常態なら、バッジは誤解を生むだけ。

### A-3. 運営者の表示
個人名か屋号か。免責の実効性と信頼性の両方に効く。
GitHubの作者は `geoAlpine`（noreplyアドレス）にしてある。

### A-4. リポジトリを公開するか
今は private。公開すると `docs/deploy.md` `docs/runbook-initial-deploy.md` の
VPS構成・ユーザー名・パスも読まれる。**公開は一方通行**なので、読み返してから。

### A-5. 初期転送量の目標
設計書は「100KB以下」だが実測 186KB（gzip）。Next.js+Reactの下限が約150KBで到達できない。
HTML 9.3KB に店名・状態・鮮度・免責が入っており、**JSが来る前に読める**状態は達成済み。
目標値を実測に合わせるか、素のSSRに作り替えるか。

### A-6. Cloudflare を入れるか（未実施）
現在はオリジン直。入れれば災害時のスパイクをキャッシュで受けられるが、
通信をCloudflareが終端するため投稿内容と投稿者IPが同社を通過する。
**入れるなら `set_real_ip_from` を最優先**。忘れると fail2ban がCloudflareの
アドレス帯をBANしてサイトが全員から見えなくなる。

### A-7. `yui-yamagata.jp` を押さえるか
空いたまま。似た名前を第三者に取られると災害時に偽サイトの入口になる。
年数千円の保険。必須ではない。

---

## B. 私が進められる実装

### B-1. 災害モードのリハーサル ★次にやるべき
ステージングで一度、災害モードへの切り替えを通しでやる。
**本番ではリハーサルできない。** 実際の災害時、緊張下で初めて触るのは危険。

確かめること: モード切替 / 告知の表示 / カテゴリが13個に増える / 通報キューの操作 /
「確認済み」の付与 / 緊急削除。

### B-2. 写真投稿
行列や貼り紙の写真1枚が持つ説得力は大きい。
クライアント側で長辺1280px・WebPに圧縮してから送る（DESIGN.md 7章）。

### B-3. 他市町村の応急給水拠点
いまは山形市28件のみ。鶴岡・酒田（令和6年豪雨の被災地）を優先したい。
公開形式が市町村ごとにバラバラなので `scripts/import_water.mjs` の `SOURCES` に足していく。

### B-4. 通報導線の拡充
いまはスポット詳細のみ。一覧のカードからも通報できるようにするか検討。

---

## C. 材料待ち

### C-1. 住民拠点SS
スクリプト（`scripts/import_juminss.mjs`）は完成し、名寄せ・ジオコーディング・
誤検出の排除まで検証済み。**エネ庁の一覧CSVを `data/` に置くだけ**。
エネ庁のサイトが自動取得を弾く（302/403）ため、手動ダウンロードが必要。

### C-2. バックアップの逃がし先
**同じVPS内はバックアップにならない。** 投稿データを日次で外部へ。
現状これが唯一の未対策リスク。

---

## D. VPSの宿題

| | 内容 |
|---|---|
| ~~D-1 カーネル~~ | ✅ 2026-08-05 完了（6.8.0-111 → 137。94日分の脆弱性を解消） |
| D-2 🟡 | 既存案件のデプロイ鍵が sudo 所属のユーザーに刺さったまま。**このプロジェクトの鍵は対策済み**（deploy-yui・command=固定）だが、既存サイトのぶんは残っている |
| D-3 🟢 | 既存案件のユーザー の `www-data` グループ所属 |
| D-4 🟢 | 用途不明のシェル付きユーザー `musicgen` / `hailo` |
| D-5 🟢 | 死んだ `proxy_pass 127.0.0.1:54321` |

**`/etc/cron.d` には immutable が付いている**（ハードニング）。
cronを足すときは `chattr -i` → 追加 → `chattr +i` の順で、**必ず戻すこと**。

VPSの契約は現状維持が正解。4GBプラン価格で6GB載っている条件は
2026-07-14の増強停止により二度と取得できない。

---

## E. 周知（技術では解決しない）

災害専用にした以上、**発見はSNS頼み**になる。イマココナビは認知ゼロから
1日7万人に届いたので、それ自体は成立する。ただし:

- 地元メディア・市町村への事前の顔つなぎがあれば、拡散は速くなる
- 「災害時にはここ」と一言でも知られていれば違う

---

## 今日踏んだ失敗（同じ轍を踏まないために）

1. **開発用シードが本番に混入** — `db/0*.sql` のglobが `002_seed_dev.sql` を拾い、
   架空の店舗が公開サイトに載った。→ `db/dev/` に隔離
2. **ビルド失敗の見逃し** — `npm run build | tail -4` はパイプなので終了コードが
   `tail` のものになり `set -e` をすり抜けた。49回クラッシュ。→ 成果物の実在を確認
3. **`/etc/yui` のディレクトリ権限** — ファイルは640にしたのにディレクトリを
   750 root:root のままにし、通れなかった（umask 027）
4. **robots.txt のビルド時焼き付き** — ステージングでビルドした成果物を本番に配ると
   本番が `Disallow: /` になる。→ `force-dynamic` にしてCIで両環境を検証
5. **cronのログ出力先** — `/var/log` は deploy-yui が書けず、毎月静かに失敗する
   状態だった。→ アプリ配下に変更し `--check` を追加

**共通するのは「失敗しても動いてしまう／気づけない」形。**
だからCIと監視で機械に見張らせている。
